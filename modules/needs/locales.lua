--- Player-facing text owned by the needs module.
-- @author dop42
--
-- Deliberately empty, and registered all the same so that a string added later
-- has one obvious home. Every word on the strip is a chip label the calling
-- module supplied, so that module is where it is translated; the only strings
-- this module owns are log lines and the codes it answers a refused call with,
-- and neither is translated.

OPX.Locale.Register('en', {})
OPX.Locale.Register('fr', {})
